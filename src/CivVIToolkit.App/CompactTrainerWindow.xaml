<Window
    x:Class="CivVIToolkit.App.CompactTrainerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:localization="using:CivVIToolkit.App.Localization">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="CompactTitleBar" Title="CivVIToolkit · 修改器" />

        <InfoBar Grid.Row="1" Margin="10,8,10,8" IsOpen="True" IsClosable="False" Severity="Informational">
            <StackPanel Spacing="2">
                <TextBlock Text="第一版 22 项修改器" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock x:Name="CompactStatusText" Text="置顶显示 · 与主窗口共享同一 Trainer 状态" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </InfoBar>

        <ListView
            x:Name="CompactTrainerList"
            Grid.Row="2"
            Margin="8,0,8,8"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="CompactTrainerList_ItemClick"
            Background="Transparent"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,1" />
                    <Setter Property="CornerRadius" Value="6" />
                    <Setter Property="MinHeight" Value="48" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="localization:LocalizedTrainerFeature">
                    <Grid MinHeight="48" Padding="9,4" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="104" />
                            <ColumnDefinition Width="116" />
                            <ColumnDefinition Width="82" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind DisplayName}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Scope}" FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Border Grid.Column="1" Width="104" Height="30" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="6" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Shortcut}" FontFamily="Cascadia Mono" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <Grid Grid.Column="2" VerticalAlignment="Center">
                            <NumberBox Tag="{x:Bind Id}" Value="{x:Bind ActionValue, Mode=OneWay}" Minimum="1" Maximum="8000000" SpinButtonPlacementMode="Compact" Visibility="{x:Bind ActionEditorVisibility}" ValueChanged="CompactValueInput_ValueChanged" Tapped="CompactValueInput_Tapped" />
                            <ToggleSwitch IsOn="{x:Bind IsEnabled, Mode=OneWay}" IsHitTestVisible="False" OffContent="" OnContent="" HorizontalAlignment="Center" Visibility="{x:Bind ToggleVisibility}" />
                        </Grid>
                        <TextBlock Grid.Column="3" Text="{x:Bind Status}" FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
