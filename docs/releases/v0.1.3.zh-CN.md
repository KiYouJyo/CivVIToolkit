v0.1.3 Preview 开始开放首个经过真实 Build 验证的写入功能：

- 对 Steam / DX12 / Gathering Storm `1.0.12.68 (1023995)` 保持精确 GameCore SHA-256 gating；
- 已验证 Gold / Faith 读取值与真实游戏 UI 一致；
- 新增 `PageUp` / 按钮执行的“增加金钱 +10,000”功能；
- 每次写入前重新执行完整只读 Profile 验证，包括 GameCore 指纹、13 个 AoB anchor、本地玩家与 Treasury 路径；
- Gold 按游戏实际的 1/256 定点格式写入，并在写入后立即回读验证；
- 其他 21 项 Trainer 功能仍保持 Signature Pending，不会使用未经验证的地址。

> 当前仍为 Preview。Trainer 仅用于 Civilization VI 本地单机模式。
