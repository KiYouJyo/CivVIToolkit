v0.1.2 Preview fixes the live Player resource object chain identified during the second real-game acceptance run.

- keep the exact Steam / DX12 / Gathering Storm `1.0.12.68 (1023995)` GameCore profile;
- confirm that `Player::Manager` returns the live `Player::Instance`, not `Player::Cache::Instance`;
- stop incorrectly dereferencing the Cache component block at `Player + 0xB0` on the live player object;
- follow the game's own bridge code and read live Religion `+0x720`, Influence `+0x748`, and Treasury `+0x780` pointers directly from the player instance;
- add runtime AoB validation for the live Faith / Gold / Influence bridge routines so the direct-component path is signature-verified as well;
- keep raw Gold / Faith / Influence fixed-point values and resolved component addresses in diagnostics for UI comparison;
- keep all Trainer writes disabled in v0.1.2 until the corrected read path matches the real game UI.

> This is still a Preview build. Trainer functionality is intended only for local Civilization VI single-player use.
