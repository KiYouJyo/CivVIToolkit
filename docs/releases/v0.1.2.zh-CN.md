v0.1.2 Preview 修正第二轮真实对局验收发现的 live Player 资源对象链问题：

- 保留 Steam / DX12 / Gathering Storm `1.0.12.68 (1023995)` 精确 GameCore Profile；
- 确认 `Player::Manager` 返回的是 live `Player::Instance`，而不是 `Player::Cache::Instance`；
- 不再错误读取 `Player + 0xB0` 的 Cache component block；
- 根据游戏自身 bridge 代码，改为从 live Player 直接读取 Religion `+0x720`、Influence `+0x748`、Treasury `+0x780` 指针；
- 新增 live Faith / Gold / Influence bridge 的运行时 AoB 验证，使 direct-component 路径也必须通过特征码认证；
- 诊断继续输出 Gold / Faith / Influence 原始定点数值和实际组件地址，供真实 UI 对照；
- v0.1.2 仍保持所有 Trainer 写入关闭，只有资源读取与 UI 数值确认一致后才开放实际修改。

> 当前仍为 Preview。Trainer 仅用于 Civilization VI 本地单机模式。
