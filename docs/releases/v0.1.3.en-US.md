v0.1.3 Preview enables the first write-capable Trainer feature validated against the real game build.

- keep exact GameCore SHA-256 gating for Steam / DX12 / Gathering Storm `1.0.12.68 (1023995)`;
- confirm Gold / Faith reads match the live game UI;
- add **Add Gold +10,000** via `PageUp` or the in-app button;
- re-run the complete read-only profile validation before every write, including the GameCore fingerprint, all 13 AoB anchors, the local player, and the Treasury path;
- write Gold using the game's native 1/256 fixed-point format and immediately read it back for verification;
- keep the remaining 21 Trainer features in Signature Pending and never write through unverified addresses.

> This is still a Preview build. Trainer functionality is intended only for local Civilization VI single-player use.
